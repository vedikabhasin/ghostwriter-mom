{
  "name": "ghostwriter-mom",
  "private": true,
  "version": "0.1.0",
  "description": "Static site + Supabase backend for ghostwriter.mom. The import script uses .mjs so it is ESM without flipping the whole package.",
  "scripts": {
    "validate": "node validate-feeds.js",
    "import": "node scripts/import-client.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  }
}
